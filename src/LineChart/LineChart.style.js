import {
    makeStyles,
} from '@mui/styles'

const useStyles = makeStyles(() => {
    return {
        chart: {
            height: '100%',
        },
        rangeFilter: {
            height: 100,
        },
    }
})

export default useStyles
