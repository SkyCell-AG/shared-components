import {
    makeStyles,
} from '@mui/styles'

const useStyles = makeStyles(() => {
    return {
        chart: {
            height: '80%',
        },
        rangeFilter: {
            height: '20%',
        },
    }
})

export default useStyles
